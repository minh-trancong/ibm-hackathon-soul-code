import type { NextPage } from "next";
import ActiveRecallPage from "@/site/ActiveRecallPage";

const ActiveRecallPageContainer: NextPage = () => {
    return <ActiveRecallPage />;
};

export default ActiveRecallPageContainer;
